# MVSplat Input

MVSplat expects chunked `.torch` data and an index file in this repo. The shared Drive folder did not contain a neighbourhood 105 `.torch` chunk dataset, only MVSplat outputs.

Use this folder for converted chunks if a DDOS-to-MVSplat converter is added.

