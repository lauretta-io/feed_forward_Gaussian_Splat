# ReSplat Input

Use the neighbourhood 105 train source:

- `../../source_train/image/`
- `../../source_train/colmap_output/`

Expected ReSplat shape is a COLMAP-style scene with images plus sparse reconstruction. The Drive COLMAP output has `database.db` and `sparse/0`.

